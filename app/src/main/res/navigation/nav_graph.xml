<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/menu2">

    <activity
        android:id="@+id/mapsMarkerActivity"
        android:name="com.example.mapwithmarker.MapsMarkerActivity"
        android:label="activity_maps"
        tools:layout="@layout/activity_maps" />
    <fragment
        android:id="@+id/menu2"
        android:name="com.example.mapwithmarker.menu"
        android:label="menu"
        tools:layout="@layout/menu" >
        <action
            android:id="@+id/action_menu2_to_mapsMarkerActivity"
            app:destination="@id/mapsMarkerActivity" />
    </fragment>
</navigation>